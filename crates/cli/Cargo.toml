[package]
name = "rura_cli"
version = "0.1.0"
edition = "2021"

[dependencies]
rura_client = { path = "../client", features = ["cli"] }
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"
serde_json = "1.0"
base64 = "0.21"
